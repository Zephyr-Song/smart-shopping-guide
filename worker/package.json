{
  "name": "bfc-agent-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret:put": "wrangler secret put LLM_API_KEY"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
