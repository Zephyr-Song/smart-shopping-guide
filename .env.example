# 前端构建时注入的环境变量（Vite 仅暴露 VITE_ 前缀的变量到浏览器）
# 复制本文件为 .env.local（已被 gitignore）并填入你的 Worker 地址：
#   cp .env.example .env.local
#
# 留空或不设置 → 小助手走「离线规则引擎」兜底（纯前端，无需任何后端）
# 填入下方地址 → 小助手切换为「真·LLM agent」（RAG + 工具调用），由 Cloudflare Worker 托管

VITE_AGENT_API_URL=https://bfc-agent.your-subdomain.workers.dev
